# Setup Tools

Host/runtime setup helpers.

Important scripts:

- `install_host_hailo_bindings.sh` — installs host Hailo Python/runtime bindings.
- `setup_local_tappas_runtime.sh` — prepares local TAPPAS runtime support.
