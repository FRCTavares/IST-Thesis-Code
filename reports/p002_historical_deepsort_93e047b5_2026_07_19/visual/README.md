# P0.2 Exact-Event Visual Evidence

Annotation intervals are loaded with the authoritative correctness evaluator.

Tracks, raw target, TIM target and status are bound by tracker frame ID.

Only the image background uses nearest header time.
The signed background-image delta is printed and recorded.

Left: preserved July 12 policy.
Right: current canonical policy.

Frame 617 is approximately 4 ms before the 36.270 s ID boundary.
Therefore its authoritative expected tracker ID remains 1.
