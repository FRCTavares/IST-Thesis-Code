# TIM-V2E embedding results

This folder stores offline descriptor and embedding experiments for selected-target identity memory.

Rules:
- Use exact eval bags and exact matching annotations.
- Do not compare annotations against regenerated tracker IDs.
- Report wrong target duration separately from lost duration.
- Report correct-time loss whenever wrong-time reduction is claimed.
- Keep live defaults unchanged unless explicitly enabled.
