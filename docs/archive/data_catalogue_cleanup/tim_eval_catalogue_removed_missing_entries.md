# Removed Missing TIM Evaluation Catalogue Entries

During the bag-root cleanup, missing rosbag paths were removed from the active
`tim_eval_catalogue.yaml`.

The active TIM evaluation catalogue should only contain existing bag paths.
Historical missing paths remain recoverable through Git history and the archived
migration manifests in this folder.
