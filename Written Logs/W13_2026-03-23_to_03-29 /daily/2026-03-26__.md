# Daily Log — 2026-03-26 (Day 26) — Dashboard Control UX + Runtime Tracker Switching

## Overview

**Focus:** Turn tracker selection into a true live control (same behavior class as model switching) and continue dashboard control-panel UX refinement.

---

## Goals for Today

### 1. Make Tracker Selection Runtime-Switchable

- [x] Implement backend API path for tracker switching
- [x] Allow tracker backend change without restart
- [x] Connect frontend controls to live tracker switch endpoint

### 2. Improve Control and Operations UX

- [x] Refine Detection and Tracking selector panel hierarchy
- [x] Improve loading and status feedback during model/tracker switches
- [x] Redesign Control tab panel to reduce dead space and increase utility

### 3. Validate Stability After Changes

- [x] Run diagnostics on touched frontend and backend files
- [x] Build frontend successfully after UI/control updates
- [x] Keep compatibility with current dashboard mode/contract flow

---

## Work Completed

### Runtime Tracker Switching (End-to-End)

- Added `POST /api/tracker` in dashboard bridge control API.
- Implemented bridge-side tracker switch handler to call tracker node parameter service (`/tracker_node/set_parameters`).
- Added supported tracker validation (`sort`, `ocsort`, `bytetrack`) and explicit error messaging.
- Updated tracker node to support live `tracker_type` updates via dynamic parameter callback.
- Rebuilt backend creation flow to safely reuse/declare tracker-specific params across runtime backend swaps.

### Frontend Control Path Wiring

- Added tracker typing and response fields in dashboard types.
- Added `requestTrackerSwitch(...)` API helper mirroring model switch flow.
- Upgraded Detection and Tracking selector:
  - tracker control enabled (no longer startup-only)
  - active model/tracker chips shown
  - stronger switching feedback and busy-state messaging
  - cleaner section card hierarchy
- Wired app state for `activeTracker`, tracker switching status, and control status reporting.

### Control Console Redesign (Control Tab)

- Reworked Control Console into structured sections:
  - Active configuration summary
  - Model controls
  - Tracker controls
  - Actions and control status
  - Backend contract block
- Added loading-aware disabling for model/tracker controls while apply operation is in flight.
- Reduced visual emptiness and improved parity with Operations panel language.

---

## Deliverables Produced

- [x] Live tracker switching path from UI -> API -> ROS tracker node
- [x] Control API expansion with `/api/tracker`
- [x] Improved Detection and Tracking selector UX
- [x] Redesigned Control Console for denser, clearer control workflow

---

## Notes and Issues

**Main insight:**

- Model and tracker controls now behave in the same interaction class, but model switching still has heavier runtime side effects (container restart) than tracker switching.

**Validation status:**

- Frontend diagnostics clean on modified files.
- Python diagnostics clean on modified backend/tracker files.
- Frontend production build successful (`tsc -b && vite build`).

**Known non-blocker:**

- Vite chunk-size warning persists (existing optimization opportunity, not a functional regression).

---

## End of Day Review

**Completed:**

- [x] Runtime tracker switching implemented and integrated
- [x] Control/operations UI quality raised with clearer interaction states
- [x] Build and diagnostics checks passed

**Time spent:** 7-9 hours

**Confidence level:** high

**Outcome:** Dashboard control surface is now materially closer to an operator-grade panel, and tracker selection is no longer a static startup-only setting.
