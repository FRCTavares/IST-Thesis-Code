# Daily Log — 2026-03-24 (Day 24) — Git Recovery, Pipeline Cleanup, and Dashboard Mode Fix

## Overview

**Focus:** Recover repository integrity, remove temporary bird-specific workflow noise from thesis docs/config, and align live dashboard behavior with person-only detections.

---

## Goals for Today

### 1. Recover Git Integrity
- [x] Diagnose `bad object HEAD` and empty object file errors
- [x] Restore healthy refs/objects without deleting workspace changes
- [x] Validate repository health after recovery

### 2. Remove Temporary Bird Workflow from Thesis Defaults
- [x] Clean bird-specific operational notes from top-level runbook/docs
- [x] Ensure temporary Birds outputs are ignored by Git
- [x] Keep live thesis pipeline defaults person-focused

### 3. Stabilize Live Dashboard Behavior
- [x] Investigate persistent bird bounding boxes in UI
- [x] Separate backend/pipeline behavior from frontend mock behavior
- [x] Set frontend defaults to backend/live mode

---

## Work Completed

### Git Recovery and Validation
- Recovered corrupted Git object database by removing zero-byte objects and force-fetching refs/objects from remote.
- Restored `main`/`HEAD` to a valid commit pointer and verified with full `git fsck`.
- Confirmed normal `git status` behavior resumed with existing local changes preserved.

### Documentation and Ignore Cleanup
- Removed the temporary bird-detection runbook section from root operational docs.
- Updated thesis-facing notes to reflect core pipeline only.
- Added `infer_service/Birds/` (and recursive content) to `.gitignore` so temporary experiment artefacts remain out of version control.

### Runtime and UI Alignment Fixes
- Hardened `infer_service/detection_zmq.py` ROS mode defaults to person-only filtering and reduced risk from stale env overrides.
- Updated `tools/start_live_stack.sh` to export `HAILO_DET_LABEL=person` and pass person label in inference client startup.
- Identified frontend root cause: dashboard defaulted to `mock` mode; mock payload was producing synthetic detections.
- Switched frontend default mode to `backend` and adjusted mock detection label to `person` for consistency.

---

## Deliverables Produced

- [x] Recovered repository from object corruption
- [x] Person-focused live stack defaults reinforced
- [x] Dashboard default mode corrected to backend/live path
- [x] Temporary Birds artefacts excluded from Git tracking

---

## Notes and Issues

**Main insight:**
- The persistent bird boxes were primarily a frontend mode mismatch (mock mode), not only a backend detector issue.

**Operational caveat:**
- Runtime behavior can still appear stale if old processes are left running; restart order remains important when validating filter changes.

---

## End of Day Review

**Completed:**
- [x] Git repaired and validated
- [x] Docs/runbook cleaned for thesis baseline
- [x] Live stack and UI defaults aligned to person-only monitoring

**Time spent:** 7-9 hours

**Confidence level:** high

**Outcome:** The thesis live workflow is cleaner, more reproducible, and less prone to confusion from temporary experimental paths.
